import { createSlice } from "@reduxjs/toolkit";

initialState


const userSlice = createSlice({

    name: "currentUser",
    initialState,
    reducers:{},
    extraReducers:(builder)=> {


    }

})